meta {
  name: List Medicines
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/medicines
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("List medicines returns an array", function() {
    const body = res.getBody();
    expect(res.getStatus()).to.equal(200);
    expect(body).to.be.an('array');
  });
}
