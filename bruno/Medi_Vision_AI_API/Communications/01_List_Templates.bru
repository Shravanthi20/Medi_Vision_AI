meta {
  name: List Communication Templates
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/communications/templates
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("List communication templates returns an array", function() {
    const body = res.getBody();
    expect(res.getStatus()).to.equal(200);
    expect(body).to.be.an('array');
  });
}
