meta {
  name: Send SMS Message
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/sms/messages/{{messageId}}/send
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("Send SMS message succeeds", function() {
    const body = res.getBody();
    expect(res.getStatus()).to.equal(200);
    expect(body).to.have.property('id');
    expect(body).to.have.property('send_status');
  });
}
