meta {
  name: List SMS Templates
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/api/sms/templates
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("List SMS templates returns an array", function() {
    const body = res.getBody();
    expect(res.getStatus()).to.equal(200);
    expect(body).to.be.an('array');
  });
}
