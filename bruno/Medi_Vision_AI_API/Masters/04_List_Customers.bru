meta {
  name: List Customers
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/customers
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("List customers returns an array", function() {
    const body = res.getBody();
    expect(res.getStatus()).to.equal(200);
    expect(body).to.be.an('array');
  });
}
